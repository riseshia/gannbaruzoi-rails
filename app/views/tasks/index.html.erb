<p id="notice"><%= notice %></p>

<h1>Tasks</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Estimated pomodoro</th>
      <th>Done flg</th>
      <th>Parent task</th>
      <th>Logged</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.description %></td>
        <td><%= task.estimated_pomodoro %></td>
        <td><%= task.done_flg %></td>
        <td><%= task.parent_task %></td>
        <td><%= task.pomodoros.count %></td>
        <td><%= link_to 'Log', log_task_path(task), method: :patch %></td>
        <td><%= link_to_unless task.done?, 'Done', done_task_path(task), method: :patch %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %>
